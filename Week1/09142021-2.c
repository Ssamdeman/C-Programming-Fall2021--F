#include <stdio.h>

int main (){
    int count =0;
    char Counter = 0;//live count 
    int ch; // varaible to store chracter road;

//Shift+option  +A is the comment things.  
    /* do {
        ch = getchar();
        if (ch == '\n') count++;

        if (ch == EOF)
            if(charCounter > 0) ++count;
            else break;


    } */
    }

//this code is mess too
   /*  
    while(ch != EOF){

        if (ch == '\n'){
            count ++;
        }
        else ch = getchar();
    }
    */

///This is mess code.
   /* for (ch = getchar();ch != EOF; ++count) {
        if (ch == '\n'){ //seen  a new line
            count++; // increment the line
            ch = getchar();  
        }
        ch = getchar(); 
        else ch = getchar();

    }   
    */